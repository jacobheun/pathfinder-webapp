div#chat-container.col-md-4.col-lg-4(ng-controller="chatController")
  h2 Chat
  ul
    li(ng-repeat="chat in chats")
      span {{chat}}
  input(ng-model="chatText" ui-keydown="{esc: 'clear($event)', enter: 'addChat()'}")
  span(class="example") */d20+2 attack morningstar,2d8+4 damage
  button(ng-Click="addChat()") Add Chat